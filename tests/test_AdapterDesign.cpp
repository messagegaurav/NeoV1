#include <gtest/gtest.h>
#include "AdapterDesign.h"

TEST(AdapterDesignTest, AdapterWorks) {
    // Example: Replace with actual class and method names
    // Adapter adap;
    // EXPECT_EQ(adap.request(), expected_value);
}