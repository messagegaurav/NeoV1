#include <gtest/gtest.h>
#include "timer.h"

TEST(TimerTest, MeasuresTime) {
    // Example: Replace with actual class and method names
    // Timer t;
    // t.start();
    // // ... do something ...
    // t.stop();
    // EXPECT_GT(t.elapsed(), 0);
}