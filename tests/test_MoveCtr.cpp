#include <gtest/gtest.h>
#include "MoveCtr.h"

TEST(MoveCtrTest, MoveSemantics) {
    // Example: Replace with actual class and method names
    // MoveCtr a;
    // MoveCtr b = std::move(a);
    // EXPECT_TRUE(b.isMoved());
}