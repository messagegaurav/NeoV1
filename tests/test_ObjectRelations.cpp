#include <gtest/gtest.h>
#include "ObjectRelations.h"

TEST(ObjectRelationsTest, RelationsWork) {
    // Example: Replace with actual class and method names
    // ObjectRelations rel;
    // EXPECT_TRUE(rel.checkRelation());
}