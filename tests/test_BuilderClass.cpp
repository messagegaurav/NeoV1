#include <gtest/gtest.h>
#include "BuilderClass.h"

TEST(BuilderClassTest, BuildObject) {
    // Example: Replace with actual class and method names
    // Builder builder;
    // auto obj = builder.build();
    // EXPECT_TRUE(obj.isValid());
}