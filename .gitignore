# Ignore everything by default
*

# Keep directory structure
!*/

# Keep C/C++ sources and headers
!*.c
!*.cc
!*.cxx
!*.cpp
!*.h
!*.hh
!*.hpp
!*.hxx
!*.ixx
!*.tpp

# Keep build scripts and config you may need
!CMakeLists.txt
!.gitignore

# (Optional) keep other text sources
!*.md

# Ignore CodeQL build artifacts
_codeql_build_dir/

# If you already committed unwanted files, run:
# git rm -r --cached .
# git add .
# git commit -m "Apply source-only .gitignore"
